{
  "name": "@chaos-garden/frontend",
  "version": "1.0.0",
  "description": "Astro frontend for Chaos Garden simulation",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "backend": "echo '🌿 Frontend: This script should be run from the root workspace' && exit 0",
    "frontend": "npm run dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist",
    "clean": "rm -rf dist .astro",
    "type-check": "astro check",
    "db:init:local": "echo '🎨 Frontend: No database to initialize - this script is handled by the workers workspace' && exit 0"
  },
  "dependencies": {
    "@chaos-garden/shared": "file:../shared"
  },
  "devDependencies": {
    "@astrojs/check": "^0.3.4",
    "@astrojs/tailwind": "^5.0.3",
    "astro": "^4.0.0",
    "tailwindcss": "^3.3.6",
    "typescript": "^5.9.3",
    "wrangler": "^4.63.0"
  }
}
