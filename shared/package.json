{
  "name": "@chaos-garden/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Chaos Garden ecosystem",
  "private": true,
  "main": "types.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "echo '🧹 Shared: No build artifacts to clean'",
    "dev": "echo '🌿 Shared: No dev server needed' && exit 0",
    "backend": "echo '🌿 Shared: This script should be run from the root workspace' && exit 0",
    "frontend": "echo '🎨 Shared: This script should be run from the root workspace' && exit 0",
    "db:init:local": "echo '🌿 Shared: No database to initialize' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "wrangler": "^4.63.0"
  }
}
